language: php

sudo: false

php:
  - 5.6
  - 7.0
  - 7.1

global:
  - REPORT_EXIT_STATUS=1

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev

script:
  - vendor/bin/phpunit

matrix:
  fast_finish: true
